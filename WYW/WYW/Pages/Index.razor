@page "/"
@using Microsoft.Extensions.Logging
@using WYW.Data
@inject IJSRuntime JSRuntime;
@using BlazorStrap;

<BSTabGroup ShowEvent="@Show" HiddenEvent="@Hidden" HideEvent="@Hide" ShownEvent="@Shown">
    <BSTabList>
        <BSTab>
            <BSTabLabel>Moje Loty</BSTabLabel>
            <BSTabContent>
             Moje Loty            
            </BSTabContent>
        </BSTab>
        <BSTab>
            <BSTabLabel>Przyloty</BSTabLabel>
                <BSTabContent>
                    <BSTable>
                        <BS_thead>
                            <BS_tr>
                                <BS_th scope="col">FLIGHT NR</BS_th>
                                <BS_th scope="col">DESTINATION</BS_th>
                                <BS_th scope="col">TIME</BS_th>
                            </BS_tr>
                        </BS_thead>
                        <BS_tbody>
                            @foreach (var record in ApiService.RecentResponse.LastResponse)
                            {
                                @if (record.type == "arrival")
                                {
                                    <BS_tr>
                                        <BS_td>@record.flight.iataNumber</BS_td>
                                        <BS_td>@record.arrival.iataCode</BS_td>
                                        <BS_td>@record.arrival.scheduledTime.ToLocalTime()</BS_td>
                                    </BS_tr>
                                }
                            }
                        </BS_tbody>
                    </BSTable>
                </BSTabContent>
            </BSTab>
            <BSTab>
                <BSTabLabel>Odloty</BSTabLabel>
                <BSTabContent>
                    <BSTable>
                        <BS_thead>
                            <BS_tr>
                                <BS_th scope="col">FLIGHT NR</BS_th>
                                <BS_th scope="col">DESTINATION</BS_th>
                                <BS_th scope="col">TIME</BS_th>
                            </BS_tr>
                        </BS_thead>
                        <BS_tbody>
                            @foreach (var record in ApiService.RecentResponse.LastResponse)
                            {
                                @if (record.type == "departure")
                                {
                                    <BS_tr>
                                        <BS_td>@record.flight.iataNumber</BS_td>
                                        <BS_td>@record.arrival.iataCode</BS_td>
                                        <BS_td>@record.arrival.scheduledTime.ToLocalTime()</BS_td>
                                    </BS_tr>
                                }
                            }
                        </BS_tbody>
                    </BSTable>
                </BSTabContent>
        </BSTab>
    </BSTabList>
    <BSTabSelectedContent/>
</BSTabGroup>
